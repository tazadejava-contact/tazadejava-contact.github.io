/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/tazadejava-contact.github.io",
    images: {
        unoptimized: true,
    }
};

export default nextConfig;
